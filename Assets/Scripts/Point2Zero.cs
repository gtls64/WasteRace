using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Point2Zero : MonoBehaviour

{
    // Start is called before the first frame update
    void Start()
    {
        SpeedPoints.TheSpeedPoints = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
